
# decrypt contact
```
curl -s https://huafengxi.github.io/contact.gpg | gpg -d  # the 1000th prime number encode in base64
```

# decrypt memo
```
curl -s https://huafengxi.github.io/memo.gpg | gpg -d  # lift my son
```
